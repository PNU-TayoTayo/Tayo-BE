

springdoc:
  packages-to-scan: pnu.cse.TayoTayo
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: / # Swagger UI ?? => localhost:8031/chat-ui.html
    tags-sorter: alpha # alpha : ??? ? ?? ??, method: HTTP method ? ??
    operations-sorter: alpha # alpha : ??? ? ?? ??, method: HTTP method ? ??
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
    cache:
      disabled: true



spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tayotayo?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234

spring.jpa:
  database: mysql
  hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  hibernate.ddl-auto: create
  #hibernate.ddl-auto: update
  hibernate.format_sql: true
  hibernate.show-sql: true